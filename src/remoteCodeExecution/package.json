{
  "name": "remotecodeexecution",
  "version": "1.0.0",
  "description": "Remote code execution system",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage",
    "test:unit": "jest --testPathPattern=__tests__/unit/ --coverage --forceExit",
    "test:integration": "jest --testPathPattern=__tests__/integration/ --coverage --forceExit",
    "test:watch": "jest --coverage --watchAll",
    "server": "node server",
    "worker": "node worker",
    "dev:server": "nodemon server",
    "dev:worker": "nodemon worker"
  },
  "author": "Janesh Sharma & Paul McNally",
  "license": "ISC",
  "dependencies": {
    "bull": "^3.22.0",
    "cors": "^2.8.5",
    "dockerode": "^3.3.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "js-base64": "^3.6.0",
    "memory-streams": "^0.1.3",
    "throng": "^5.0.0"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "nodemon": "^2.0.7",
    "supertest": "^6.1.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
